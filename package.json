{
  "name": "usft-monorepo",
  "packageManager": "yarn@3.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint": "^8.15.0",
    "husky": ">=6",
    "lint-staged": ">=10",
    "prettier": "^2.6.2"
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix",
    "*.{js,css,md}": "prettier --write"
  },
  "scripts": {
    "postinstall": "husky install"
  }
}
